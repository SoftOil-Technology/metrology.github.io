---
interface Props {
  numero: string;
  titulo: string;
  descripcion: string;
}

const { numero, titulo, descripcion } = Astro.props;
---

<div class="bg-white p-4 sm:p-6 rounded-lg shadow-lg border-t-4 border-primary hover:shadow-xl transition-all duration-300 flex flex-col h-full">
  <div class="flex items-center mb-3 sm:mb-4">
    <div class="bg-primary text-white rounded-full w-10 h-10 sm:w-12 sm:h-12 flex items-center justify-center text-lg sm:text-xl font-bold mr-3 sm:mr-4 shrink-0">
      {numero}
    </div>
    <h3 class="text-lg sm:text-xl font-bold text-primary leading-tight">{titulo}</h3>
  </div>
  <p class="text-sm sm:text-base text-gray-700 flex-grow leading-relaxed">{descripcion}</p>
</div>